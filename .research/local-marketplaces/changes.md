# Changes

## 2026-08-18 — created

Opened while adding a `marketplace` skill, prompted by a third-party README that publishes install
commands for four runtimes. Verified each command against vendor documentation. Two held, one is
uncorroborated (Codex CLI marketplace verbs), and one does not exist (a Cursor repository-local
marketplace). Also recorded that this project's Codex catalog path and schema have no source.
