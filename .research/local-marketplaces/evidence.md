# Evidence — Local marketplaces and install instructions

| ID | Claim | Source | Date | Status | Confidence |
|---|---|---|---|---|---|
| E-CC-M1 | A marketplace repo carries `.claude-plugin/marketplace.json` at its root. "Create `.claude-plugin/marketplace.json` in your repository root." | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M2 | Users add a marketplace with `/plugin marketplace add owner/repo` and install with `/plugin install <plugin>@<marketplace>`. Worked example: `/plugin marketplace add ./my-marketplace`, `/plugin install quality-review-plugin@my-plugins`. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M3 | Non-interactive installs exist: `claude plugin install` / `claude plugin update` accept `--yes` in a non-interactive shell. Users refresh a catalog with `/plugin marketplace update`. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M4 | Plugin `source` paths resolve relative to the marketplace root, the directory containing `.claude-plugin/`. Relative paths do not resolve when a marketplace is added by direct URL to the JSON file. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-COPILOT-M1 | `copilot plugin marketplace add SPECIFICATION` registers a marketplace. The marketplace's own `name` from `marketplace.json` becomes the registration key; a custom local name cannot be set. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-COPILOT-M2 | `copilot plugin install SPECIFICATION` accepts `plugin@marketplace`, `OWNER/REPO`, `OWNER/REPO:PATH`, a Git URL, or a local path. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-COPILOT-M3 | Marketplace manifests are read as `marketplace.json` in the root, `.plugin/`, `.github/plugin/`, or `.claude-plugin/`. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-CODEX-M1 | Codex CLI installs plugins through a browser: "In Codex CLI, enter `/plugins` to open the plugin browser. Install a plugin from a configured marketplace, then start a new session." No `plugin marketplace add` CLI verb appears. | https://learn.chatgpt.com/docs/plugins | 2026-08-18 | verified | High (for what the page says) |
| E-CODEX-M2 | A Codex plugin manifest is `.codex-plugin/plugin.json`. The page describes adding a folder to a local marketplace with `@plugin-creator` / `$plugin-creator`, and documents no marketplace manifest format. | https://learn.chatgpt.com/docs/build-plugins | 2026-08-18 | verified | High (for what the page says) |
| E-CODEX-M3 | `codex plugin marketplace add <SOURCE>` exists and takes "a local path, owner/repo[@ref], HTTPS Git URL, or SSH Git URL". Sibling verbs: `list`, `upgrade`, `remove`. Installation is `codex plugin add <plugin>@<marketplace>`; there is no `codex plugin install`. | `codex plugin --help`, `codex plugin marketplace add --help`, codex-cli 0.147.0 | 2026-08-18 | verified | High — the shipped CLI, which the docs pages do not cover |
| E-CODEX-M4 | ~~Codex reads only `.claude-plugin/marketplace.json`.~~ **Retracted.** The fixture for `.agents/plugins/` was named `<name>.json`, not `marketplace.json`, so the probe tested a filename rather than a directory. Superseded by E-CODEX-M10. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | **retracted** | — |
| E-CODEX-M5 | ~~The Codex catalog this project writes is not read by Codex.~~ **Retracted**, and it was wrong: see E-CODEX-M11. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | **retracted** | — |
| E-CODEX-M6 | End-to-end install works from a local marketplace: `codex plugin add demo@probe-claude` reported "Added plugin `demo`" and installed to `~/.codex/plugins/cache/<marketplace>/<plugin>/<version>`. The plugin was found through a `./`-prefixed relative `source` and carried `.codex-plugin/plugin.json`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M7 | `codex plugin list` prints the manifest path it read for each marketplace, e.g. `~/.codex/.tmp/marketplaces/cyberuni/.claude-plugin/marketplace.json`. Two pre-existing local marketplaces on this machine carry only `.claude-plugin/marketplace.json`. | `codex plugin list`, `codex plugin marketplace list` | 2026-08-18 | verified | High |
| E-CODEX-M8 | Codex distinguishes a missing manifest from a bad one. A broken catalog at a supported path errors as ``invalid marketplace file `<root>/.claude-plugin/marketplace.json`: missing field `name` at line 1 column 14`` — naming the file. An unsupported path errors as ``invalid marketplace file `<root>`: marketplace root does not contain a supported manifest`` — naming the directory. `.codex-plugin/marketplace.json` produces the second form, so the file is never opened. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M9 | Codex accepts the catalog shape this project writes for `.agents/plugins` when it sits at `.claude-plugin/marketplace.json`: `interface`, `policy`, and `category` are tolerated, `owner` is not required, and an object `source` of `{"source":"local","path":"./…"}` resolves. `codex plugin add` installed from it. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CC-M5 | Claude Code requires `owner`. Adding the Codex-shaped catalog fails: `Invalid schema: … owner: Invalid input: expected object, received undefined`. The Claude shape (`name`, `owner`, `plugins` with `./`-prefixed string sources) is accepted by Claude Code and by Codex, so it is the common subset. | `claude plugin marketplace add`, Claude Code CLI | 2026-08-18 | verified | High |
| E-CODEX-M10 | Codex discovers a marketplace by the filename `marketplace.json` inside a supported directory. Accepted: `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`. Rejected, with the correct filename in each: `.codex-plugin/marketplace.json`, `.plugin/marketplace.json`, `.github/plugin/marketplace.json`, and root `marketplace.json`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M11 | The catalog `marketplace init --codex` writes is read by Codex and installs from end to end. `codex plugin marketplace add` accepted the fixture, `codex plugin list` printed `.agents/plugins/marketplace.json` as the manifest it read, and `codex plugin add demo@probe-exact` installed to `~/.codex/plugins/cache/probe-exact/demo/1.0.0`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CUR-M2 | `cursor-agent` 2026.07.01-41b2de7 exposes no plugin or marketplace subcommand. Its commands are login, logout, mcp, worker, status, models, bedrock, about, update, create-chat, generate-rule, agent, ls, resume. | `cursor-agent --help` | 2026-08-18 | verified | High |
| E-CUR-M1 | ~~Cursor installs plugins from the Customize sidebar, sourced from the reviewed marketplace, cursor.directory, or a team marketplace. Local development uses `~/.cursor/plugins/local/<name>`. No repository-local marketplace file format is documented.~~ **Narrowed.** The install and local-development halves hold. The last sentence was read off the install page while the format sits on the reference page; superseded by E-CUR-M6. | https://cursor.com/docs/plugins | 2026-08-18 | **narrowed** | — |
| E-CUR-M6 | Cursor documents `.cursor-plugin/marketplace.json` at the repository root for multi-plugin repositories, corroborating E-CUR-M5 from the vendor's own page. Required: `name` (kebab-case), `owner` (object; `name` required, `email` optional), `plugins` (array, max 500). Optional `metadata` carries `description`, `version`, and `pluginRoot`, "prefix path for all plugin sources". An entry requires `name` and `source` ("Path to plugin directory, or object with `path` and options"); `description`, `version`, `author`, `homepage`, `repository`, `license`, `keywords`, `logo`, `category`, `tags`, and component paths are optional. `$schema` is not documented. | https://cursor.com/docs/reference/plugins | 2026-08-18 | verified | High |
| E-CUR-M7 | A repository catalog reaches Cursor users through a team marketplace an admin imports, not through anything the author runs: "Go to **Dashboard -> Plugins**. In **Team Marketplaces**, click **Add Marketplace**. … use \"Import from Repo\" if importing from GitHub." Auto Refresh then updates plugins "whenever changes are pushed to the branch the marketplace tracks", reindexing at most every 10 minutes, and picks up newly added plugins only on re-import. | https://cursor.com/docs/plugins | 2026-08-18 | verified | High |
| E-CC-M8 | Claude Code requires `owner` to be an object. A string fails validation — `owner: Invalid input: expected object, received string` — and exits non-zero; the object form with `name` alone passes. The catalogs this project generated carried a string. | `claude plugin validate`, Claude Code 2.1.235 | 2026-08-18 | verified | High |
| E-CC-M9 | A catalog entry's `source` may point at a directory whose manifest is a canonical root `plugin.json` rather than `.claude-plugin/plugin.json`; validation passes. A `$schema` key on the catalog is accepted, and documented as ignored at load time, as is an entry that declares no `version`. | `claude plugin validate`, Claude Code 2.1.235; https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M6 | Claude Code has no `~/.claude/plugins/local/` directory. The string `plugins/local` does not appear anywhere in the shipped executable, and `~/.claude/plugins/` holds only `cache/`, `data/`, `marketplaces/`, `installed_plugins.json`, and `known_marketplaces.json`. The symlink recipe published in this project's skill docs targeted a path no runtime reads. | `strings` over the Claude Code 2.1.235 executable; `ls ~/.claude/plugins` | 2026-08-18 | verified | High |
| E-CC-M7 | Claude Code adopts a plugin directory placed under its skills directory — `~/.claude/skills/<dir>` for the user, `.claude/skills/<dir>` for a project — and loads it as `<name>@skills-dir`, where `<name>` comes from the plugin's manifest rather than from the directory. A **symlink** whose target lies outside the skills directory is followed. Probed with `CLAUDE_CONFIG_DIR` pointed at a sandbox: `claude plugin list` reported `universal-plugin@skills-dir … Status: loaded` against the symlink's own path. `claude plugin init <name>` scaffolds into the same place, so this is the vendor's own local-development path. | direct experiment, Claude Code 2.1.235; `claude plugin init --help` | 2026-08-18 | verified | High |
| E-CUR-M3 | Cursor scans `~/.cursor/plugins/local/` and adopts each entry that is a directory or a symlink, skipping dot-prefixed names (`loadUserLocalPlugins` in the shipped bundle). | source read, cursor-agent 2026.07.01-41b2de7 | 2026-08-18 | verified | High |
| E-CUR-M4 | That scan **rejects a symlink whose resolved target sits outside `~/.cursor/plugins/local/`**. `loadUserLocalPlugins` resolves each symlink with `realpath` and then runs a containment check against the scanned directory, logging `loadUserLocalPlugin <name> rejected: symlink target <path> is outside <dir>` and skipping the entry. The `ln -sfn "$(pwd)" ~/.cursor/plugins/local/<name>` recipe published in this project's skill docs therefore installs nothing. A plain directory is never checked, so a copy loads. | source read, cursor-agent 2026.07.01-41b2de7 | 2026-08-18 | verified | Medium-high — read from the shipped bundle; the containment check is unambiguous, but it was not exercised end to end, and Cursor's IDE loader was not inspected |
| E-CUR-M5 | Cursor does read a repository-local marketplace, contrary to this topic's earlier verdict. The shipped bundle carries the manifest list `[".cursor-plugin/marketplace.json", ".claude-plugin/marketplace.json"]` alongside the plugin-manifest list `[".cursor-plugin/plugin.json", ".claude-plugin/plugin.json"]`, and resolves marketplace checkouts under `~/.cursor/plugins/marketplaces`. | source read, cursor-agent 2026.07.01-41b2de7; https://cursor.com/docs/reference/plugins | 2026-08-18 | verified | Medium-high — read from the shipped bundle; no end-to-end install was run |
| E-CODEX-M12 | Codex scans no local plugin directory. The shipped binary carries `plugins/cache` and no `plugins/local` equivalent, and `codex plugin --help` offers only marketplace-sourced `add` / `remove`. Local development goes through `codex plugin marketplace add <local path>` (E-CODEX-M3). | `strings` over codex 0.147.0; `codex plugin --help` | 2026-08-18 | verified | High |
| E-CODEX-M13 | A local install is a **copy**, keyed by version: `codex plugin add` writes `<CODEX_HOME>/plugins/cache/<marketplace>/<plugin>/<version>/` and reports that path as "Installed plugin root". The copied files are ordinary files, not symlinks, so an edit under the marketplace source is invisible to the installed plugin until the plugin is installed again. | direct experiment, codex-cli 0.147.0 (isolated `CODEX_HOME`) | 2026-08-18 | verified | High |
| E-CODEX-M14 | Re-running `codex plugin add <plugin>@<marketplace>` at the **same** version re-copies the source: after editing a skill body and re-running `add`, the cached copy carried the new body. Refreshing a local plugin needs neither `codex plugin remove` nor a version bump. `codex plugin marketplace upgrade` does not do it — it reports "No configured Git marketplaces to upgrade" for a local marketplace. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M15 | The version in the cache path comes from the **plugin's own manifest**, not from the catalog entry. With the catalog entry at `1.2.0` and the plugin manifest at `1.0.0`, `codex plugin add` installed into `…/demo/1.0.0`; moving the manifest to `1.2.0` installed into `…/demo/1.2.0`. `codex plugin list` prints a version only once installed, and prints the manifest's. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High — probed for `local` sources only |
| E-CODEX-M16 | A catalog entry that declares **no** `version` installs normally (the manifest supplies it). A plugin **manifest** with no version installs too, into the literal directory `…/<plugin>/local`. Codex therefore never requires a version on a local catalog entry. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M17 | Installing a plugin at a new version replaces the previously cached version directory rather than accumulating beside it: after installing `1.0.0` and then `1.2.0`, `…/cache/<marketplace>/demo/` held `1.2.0` alone. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M18 | Codex reads a plugin's manifest from `.codex-plugin/plugin.json` or from a canonical root `plugin.json`, and the `.codex-plugin` one wins where both exist: with `5.0.0` in root and `4.0.0` in `.codex-plugin`, the install landed in `…/demo/4.0.0`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M19 | `codex plugin remove <plugin>` refuses a bare plugin name: "plugin requires --marketplace unless passed as `<plugin>@<marketplace>`". | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
