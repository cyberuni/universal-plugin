# Evidence — Local marketplaces and install instructions

| ID | Claim | Source | Date | Status | Confidence |
|---|---|---|---|---|---|
| E-CC-M1 | A marketplace repo carries `.claude-plugin/marketplace.json` at its root. "Create `.claude-plugin/marketplace.json` in your repository root." | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M2 | Users add a marketplace with `/plugin marketplace add owner/repo` and install with `/plugin install <plugin>@<marketplace>`. Worked example: `/plugin marketplace add ./my-marketplace`, `/plugin install quality-review-plugin@my-plugins`. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M3 | Non-interactive installs exist: `claude plugin install` / `claude plugin update` accept `--yes` in a non-interactive shell. Users refresh a catalog with `/plugin marketplace update`. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-CC-M4 | Plugin `source` paths resolve relative to the marketplace root, the directory containing `.claude-plugin/`. Relative paths do not resolve when a marketplace is added by direct URL to the JSON file. | https://code.claude.com/docs/en/plugin-marketplaces | 2026-08-18 | verified | High |
| E-COPILOT-M1 | `copilot plugin marketplace add SPECIFICATION` registers a marketplace. The marketplace's own `name` from `marketplace.json` becomes the registration key; a custom local name cannot be set. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-COPILOT-M2 | `copilot plugin install SPECIFICATION` accepts `plugin@marketplace`, `OWNER/REPO`, `OWNER/REPO:PATH`, a Git URL, or a local path. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-COPILOT-M3 | Marketplace manifests are read as `marketplace.json` in the root, `.plugin/`, `.github/plugin/`, or `.claude-plugin/`. | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-plugin-reference | 2026-08-18 | verified | High |
| E-CODEX-M1 | Codex CLI installs plugins through a browser: "In Codex CLI, enter `/plugins` to open the plugin browser. Install a plugin from a configured marketplace, then start a new session." No `plugin marketplace add` CLI verb appears. | https://learn.chatgpt.com/docs/plugins | 2026-08-18 | verified | High (for what the page says) |
| E-CODEX-M2 | A Codex plugin manifest is `.codex-plugin/plugin.json`. The page describes adding a folder to a local marketplace with `@plugin-creator` / `$plugin-creator`, and documents no marketplace manifest format. | https://learn.chatgpt.com/docs/build-plugins | 2026-08-18 | verified | High (for what the page says) |
| E-CODEX-M3 | `codex plugin marketplace add <SOURCE>` exists and takes "a local path, owner/repo[@ref], HTTPS Git URL, or SSH Git URL". Sibling verbs: `list`, `upgrade`, `remove`. Installation is `codex plugin add <plugin>@<marketplace>`; there is no `codex plugin install`. | `codex plugin --help`, `codex plugin marketplace add --help`, codex-cli 0.147.0 | 2026-08-18 | verified | High — the shipped CLI, which the docs pages do not cover |
| E-CODEX-M4 | ~~Codex reads only `.claude-plugin/marketplace.json`.~~ **Retracted.** The fixture for `.agents/plugins/` was named `<name>.json`, not `marketplace.json`, so the probe tested a filename rather than a directory. Superseded by E-CODEX-M10. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | **retracted** | — |
| E-CODEX-M5 | ~~The Codex catalog this project writes is not read by Codex.~~ **Retracted**, and it was wrong: see E-CODEX-M11. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | **retracted** | — |
| E-CODEX-M6 | End-to-end install works from a local marketplace: `codex plugin add demo@probe-claude` reported "Added plugin `demo`" and installed to `~/.codex/plugins/cache/<marketplace>/<plugin>/<version>`. The plugin was found through a `./`-prefixed relative `source` and carried `.codex-plugin/plugin.json`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M7 | `codex plugin list` prints the manifest path it read for each marketplace, e.g. `~/.codex/.tmp/marketplaces/cyberuni/.claude-plugin/marketplace.json`. Two pre-existing local marketplaces on this machine carry only `.claude-plugin/marketplace.json`. | `codex plugin list`, `codex plugin marketplace list` | 2026-08-18 | verified | High |
| E-CODEX-M8 | Codex distinguishes a missing manifest from a bad one. A broken catalog at a supported path errors as ``invalid marketplace file `<root>/.claude-plugin/marketplace.json`: missing field `name` at line 1 column 14`` — naming the file. An unsupported path errors as ``invalid marketplace file `<root>`: marketplace root does not contain a supported manifest`` — naming the directory. `.codex-plugin/marketplace.json` produces the second form, so the file is never opened. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M9 | Codex accepts the catalog shape this project writes for `.agents/plugins` when it sits at `.claude-plugin/marketplace.json`: `interface`, `policy`, and `category` are tolerated, `owner` is not required, and an object `source` of `{"source":"local","path":"./…"}` resolves. `codex plugin add` installed from it. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CC-M5 | Claude Code requires `owner`. Adding the Codex-shaped catalog fails: `Invalid schema: … owner: Invalid input: expected object, received undefined`. The Claude shape (`name`, `owner`, `plugins` with `./`-prefixed string sources) is accepted by Claude Code and by Codex, so it is the common subset. | `claude plugin marketplace add`, Claude Code CLI | 2026-08-18 | verified | High |
| E-CODEX-M10 | Codex discovers a marketplace by the filename `marketplace.json` inside a supported directory. Accepted: `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`. Rejected, with the correct filename in each: `.codex-plugin/marketplace.json`, `.plugin/marketplace.json`, `.github/plugin/marketplace.json`, and root `marketplace.json`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CODEX-M11 | The catalog `marketplace init --codex` writes is read by Codex and installs from end to end. `codex plugin marketplace add` accepted the fixture, `codex plugin list` printed `.agents/plugins/marketplace.json` as the manifest it read, and `codex plugin add demo@probe-exact` installed to `~/.codex/plugins/cache/probe-exact/demo/1.0.0`. | direct experiment, codex-cli 0.147.0 | 2026-08-18 | verified | High |
| E-CUR-M2 | `cursor-agent` 2026.07.01-41b2de7 exposes no plugin or marketplace subcommand. Its commands are login, logout, mcp, worker, status, models, bedrock, about, update, create-chat, generate-rule, agent, ls, resume. | `cursor-agent --help` | 2026-08-18 | verified | High |
| E-CUR-M1 | Cursor installs plugins from the Customize sidebar, sourced from the reviewed marketplace, cursor.directory, or a team marketplace. Local development uses `~/.cursor/plugins/local/<name>`. No repository-local marketplace file format is documented. | https://cursor.com/docs/plugins | 2026-08-18 | verified | High |
