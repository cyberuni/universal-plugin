{"kind": "correction", "cr": "github-67", "ts": "2026-09-08T04:49:06Z", "correction-kind": "judge-iteration", "cause": "coverage-gap", "note": "Round 1 blocked at the governance pre-flight: the inline spec-producer declared no governances loaded. Loaded all seven, re-aligned the draft against them, and re-submitted. Two suite defects the bars caught before the judge saw them: an absent-state Given that no two readers could build the same fixture from, and a lone negative with no positive companion on the same path."}
{"kind": "correction", "cr": "github-67", "ts": "2026-09-08T04:49:06Z", "correction-kind": "judge-iteration", "cause": "coverage-gap", "note": "Round 2 Builder FAIL: the namespace component-copy is a newly-introduced operation and shipped a happy path only — no declared-but-missing directory, no non-string pathValue form, no undeclared-default case, while the sibling skills resolution states and tests all three. Fixed by stating the resolution contract for the new fields and covering each edge, in the spec and in the build. Also picked up the judge's architect observation: ADR-0004's Copilot premise expired with this CR and had not been given the supersession note its sibling ADR-0011 got in the same diff."}
{"kind": "correction", "cr": "github-67", "ts": "2026-09-08T05:04:37Z", "correction-kind": "judge-iteration", "cause": "coverage-gap", "note": "Round 3 Builder + Architect FAIL, four findings, all upheld. Two under-discriminated edges: the AXI aggregate suffix was tested only on the branch where it appears, though copilot-cli's status is now conditional and a build that still assumed it canonical would pass; and one scenario claimed to cover every pathValue form while exercising one ambiguous shape, where the sibling skills resolution splits array and paths-object. Both split and covered on both branches. One self-contradiction: the supersession note added to ADR-0011 section 3 left the paragraph it superseded standing as a live claim, and mis-described what survived — the surviving rule belongs to the new ADR and never depended on the old one. The stale paragraph is now bracketed as historical. One miscitation: the agent filename rule cited the same published page this ADR's own context calls stale; re-cited to the page that documents it, and recorded as new evidence."}
{"kind": "correction", "cr": "github-67", "ts": "2026-09-08T05:11:41Z", "correction-kind": "judge-iteration", "cause": "coverage-gap", "note": "Round 4 cleared all four round-3 findings on their merits and swept for the rule behind them, finding a third instance: the ADR claims the vendor reports built when it derives any of five kinds, and only the agents branch asserted the status. A build gating built on the copied kinds alone would have reported a hooks-only or lsp-only plugin as canonical — the same silent loss this CR ends, moved into the status column. The implementation was already right; the suite did not say so. Covered for all four remaining kinds, and the ADR now states that any kind flips it."}
