{"kind": "leash", "cr": "bundle-emit-pins-map", "leash": "auto-all", "by": "user", "approach": ["additive to the frozen plugin/bundle node: new scenarios asserting bundle also writes .plugin/pins.json (workspace-resolved package->version map); no frozen scenario narrowed (the 'only skill pins' line is README non-goal prose, not a scenario) -> self-clears, stays @frozen, no re-open", "README non-goal relaxed in-sync to permit the generated sibling; .plugin/plugin.json manifest + vendor outputs still untouched", "small impl: cli.ts writes the map from result.pins (pinned/unchanged), skip under --dry-run; part of the approved cyberlegion surfacing-hook plan (feeds Part C init skill)", "reversible feature branch; user green-lit the full 4-part plan; hard floors still stop"]}
{"kind": "gate", "cr": "bundle-emit-pins-map", "gate": "spec", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "Additive to the frozen plugin/bundle node: 3 new boolean scenarios (bundle writes .plugin/pins.json mapping workspace-resolved versions; --dry-run skips the write; external/skipped packages excluded). No existing frozen scenario narrowed or rewritten (gherkin-cli diff: 0 modified / 0 removed) -> self-clears under conductor authority, stays @frozen, no re-open. README synced in place: non-goal relaxed to permit the generated .plugin/pins.json sibling while .plugin/plugin.json manifest + vendor outputs stay untouched; Use Case + behavior-table row added. Status stays approved (additive detail-adjustment, no project-wide lifecycle transition).", "frozen": ["plugin/bundle/bundle.feature"]}
{"kind": "gate", "cr": "bundle-emit-pins-map", "gate": "impl", "verdict": "approve", "by": "agent", "cause": "dimension", "why": "Cold sdd-impl-judge (ADR-0016, oracle re-derived per frozen scenario) over the 3 additive Version-map scenarios: all PASS with genuine non-tautological assertions (reads pins.json off disk; --dry-run asserts file-absence not just exit 0; external-exclusion asserts both workspace-key-present AND external-key-undefined). Exercise backstop: 3 targeted mutations (write current not resolved; drop the dry-run guard; write skipped pins too) each caught the scenario, each reverted byte-identical. Clean Architecture verified: writePinsMap in fs.ts (infra I/O), cli.ts owns the dry-run wiring, pure bundle.ts untouched; no duplication/conflict. Full package suite 167/167, repo pnpm verify 19/19. Additive detail-adjustment: root status stays approved (no project-wide lifecycle transition).", "frozen": []}
