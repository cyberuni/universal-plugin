{"kind":"leash","cr":"github-15","leash":"auto-all","by":"user","approach":["Revise run/run.feature: fold the two scenarios sharing trigger 'upx tool-a@next' (dist-tag->npx routing + dist-tag miss-notice wording) into one scenario asserting the union of their Then steps. Behavior-preserving, no upx code change (unit tests already cover both behaviors independently).","Frozen-suite re-open: user ratified the re-open and pre-authorized the Clearance floor in-session; merge preserves every assertion so coverage is unchanged."]}
{"kind":"gate","cr":"github-15","gate":"spec","verdict":"approve","by":"agent","why":"run/run.feature fold: 2 same-trigger frozen scenarios ('upx tool-a@next' dist-tag->npx routing + dist-tag miss-notice wording) merged into 1. gherkin-cli diff 1 added/0 modified/2 removed — a re-open (removed frozen scenarios), user-ratified in-session with Clearance pre-authorized (auto-all leash). Coverage preserved: merged scenario asserts the union of all 4 unique Then steps (NPX-SHIM tool-a@next routing, 'no installed tool-a' prefix, 'dist-tag', not 'satisfies') — no assertion weakened, so Clearance does not actually narrow. Merged scenario is boolean/well-formed per suite-format. Feature stays @frozen. Behavior-preserving cosmetic hygiene, no code change. Self-asserted (conductor holds user channel; user ratified)."}
{"kind":"gate","cr":"github-15","gate":"impl","verdict":"approve","by":"agent","why":"No production change — the fold is suite-structure only; upx run.ts untouched. The merged frozen scenario's behaviors are independently verified by existing unit tests (run.test.ts:204 dist-tag notice wording, :265 dist-tag routing to npx) which were not modified. pnpm test green 295/295 (18 files). Passed clean, no judge-iteration correction. Self-asserted (by agent)."}
