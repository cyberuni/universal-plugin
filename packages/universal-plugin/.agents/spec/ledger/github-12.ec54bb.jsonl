{"kind":"leash","cr":"github-12","leash":"auto-spec","by":"derived","approach":["item 2 (CR, node run/): additive dist-tag-notice scenario locking notice omits 'satisfies'; split distTagNotice off fallbackNotice in run.ts","item 1 (escaped skill work, skills/adopt-upx outside project spec): rewrite-upx.mjs 0.x pins -> ^0.<minor> not ^0; keep ^<major> for >=1.0.0; update SKILL.md"]}
{"kind":"gate","cr":"github-12","gate":"spec","verdict":"approve","by":"agent","why":"run.feature +1 additive scenario locking the dist-tag miss notice (keeps 'no installed <pkg>' prefix, contains 'dist-tag', omits 'satisfies'); gherkin-cli diff 1 added/0 modified/0 removed so freeze self-clears (stays @frozen, no re-open). Cold sdd-spec-judge ALIGNED true (oracle/builder/architect all PASS) — new scenario discriminates (a naive shared-notice fix fails it), prose<->suite 1:1, no conflict with sibling @next scenario. One deferred architect observation (fold same-trigger scenarios when suite next unfrozen). Self-asserted (by agent, auto-spec leash)."}
{"kind":"gate","cr":"github-12","gate":"impl","verdict":"approve","by":"agent","why":"Cold sdd-impl-judge IMPL-GATE PASS — all 26 frozen run/ scenarios re-derived by hand (no bridge) + verified; new distTagNotice scenario passes, range-miss still contains 'satisfies' (split did not regress), bare '*' miss stays on fallbackNotice not distTagNotice. pnpm test green 253/253 (16 files, +2 vs github-10). run.ts stays pure domain; fallbackNotice/distTagNotice a clean non-duplicative split; no scope creep (only run.ts/run.test.ts/upx.test.mts/run.feature/README.md touched); absorption clean. Passed clean, no judge-iteration correction. Self-asserted (by agent)."}
{"kind":"followup","cr":"github-12","class":"backlog","summary":"run.feature has two scenarios sharing the trigger 'upx tool-a@next' (the dist-tag->npx routing scenario and the new dist-tag-notice-wording scenario). Correct as a split under @frozen (folding would narrow the frozen routing scenario), but worth merging into one scenario next time this suite area is genuinely unfrozen. Surfaced by the cold spec-judge (architect lens)."}
