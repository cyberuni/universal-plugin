{"seq":1,"kind":"leash","cr":"github-56","leash":"auto-all","by":"derived","blast":"moderate","approach":["spec-first","reuse-pin-matcher","follow-hooks-derivation-precedent"]}
{"seq":2,"kind":"gate","cr":"github-56","gate":"spec","verdict":"approve","by":"agent","cause":"dimension","frozen":["plugin/build/build.feature"],"why":{"floor":"none — the CR is additive: 16 new scenarios inserted into an already-frozen suite with zero deletions, no existing scenario narrowed, rewritten, moved, or removed. Compatibility none — a new opt-in marker on a manifest block that previously rode through as opaque JSON, inert for every plugin that does not set it. Conflict none.","blast":"moderate — one behavioral node revised, its placement recorded in the root spec, and one row added to the version-policy decision's derived-vs-authored table; no other node's contract touched.","novelty":"low — the design applies the corpus's existing author-abstractly/stamp-concretely rule (hook casing) to a second manifest field, and reuses the pin domain's existing matcher rather than deriving a second one.","confidence":"high — cold spec-judge round 3 returns all three lenses pass and ALIGNED true; the two earlier rounds' blockers (a missing positive companion for the second runner word, and a closure claim contradicting the node's own catalog-refresh behavior) were each remediated and re-judged; the suite parses and the deterministic spec-state and suite checks report clean.","cr":"github-56"}}
{"seq":3,"kind":"gate","cr":"github-56","gate":"impl","verdict":"approve","by":"agent","cause":"dimension","why":{"floor":"none — no frozen scenario weakened; every pre-existing scenario still passes.","blast":"moderate — the pin domain gains the shared runner/specifier parsing plus the mcpServers pinner, and build gains the canonical mcpServers resolution and per-vendor delivery; the skill-prose extractor was refactored onto the shared matcher with its behavior unchanged.","novelty":"low — the delivery path mirrors the hooks translation already in this file, and the domain stays pure with the application layer owning every side effect.","confidence":"high — cold impl-judge re-derived each frozen scenario's oracle independently and named the test exercising it, with no scenario left unverified; full repository verification is green at 620 tests; the CLI path is exercised end-to-end through the shipped binary.","cr":"github-56"}}
{"seq":4,"kind":"followup","cr":"github-56","class":"backlog","what":"plugin doctor could warn on an unpinned MCP self-invocation that carries no marker, narrowed to the case where the args package name equals the plugin's own npm package name (read from the declared packagePath's package.json) so the check never guesses. Held out of this CR: doctor is a skill with no behavioral node in the corpus, so the check needs its own spec node rather than riding on the build node's contract."}
{"seq":5,"kind":"followup","cr":"github-56","class":"backlog","what":"the build node's README predates the current spec-format section skeleton — it carries Use Cases plus an unheaded behavior table, with no What, Control Flow, or Scenario map heading. Corpus-wide across the behavioral nodes, raised as an observation by the cold spec-judge in every round; belongs to a formation pass, not to a behavior CR."}
