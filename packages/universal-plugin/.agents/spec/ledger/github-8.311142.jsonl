{"kind":"leash","cr":"github-8","leash":"auto-spec","by":"user","approach":["new config/ command group (peer of plugin/, governance/, run/) with config/add + config/get unit nodes; reads/writes keyed arrays of {name,...} objects in repo-root .agents/universal-plugin.json resolved from cwd","config add: require name (merge dedup key), append-or-replace-by-name in place (position preserved), create file/key if absent, preserve other top-level keys; config get: AXI TOON default keyed on name + count aggregate, --format json = raw stored array, definitive empty state","reserved key packagePath (string, read by publish sync-version) rejected by both verbs; vendors dropped from the config file as dead/unread config","charter: keep in universal-plugin (owns its own config file) with explicit placement Charter note; all-additive, nothing frozen touched; reversible feature branch","ship now: command has no dep on version-locking; install-script adoption gated on version-locking is a downstream followup"]}
{"kind":"gate","cr":"github-8","gate":"spec","verdict":"approve","by":"unional","why":"config/add + config/get frozen: cold sdd-spec-judge ALIGNED true round 3 {oracle,builder,architect all PASS, 0 findings}; 33 boolean scenarios, prose<->suite 1:1 both ways, both .feature parse clean + check-suite OK. R1 caught exit-code discrimination + missing table rows + unfalsifiable replace-position + reserved-key gap; R2 caught 4 stale-vendors prose refs; both fixed, grep-verified zero vendors in config/+axi. Reserved key packagePath rejected (string, read by publish sync-version); vendors dropped as dead config. Floors: Clearance n/a (all-new, nothing frozen touched), Compatibility n/a (impl unbuilt, no shipped bump), Conflict none. Charter: keep in-package (owns its own config file) + placement Charter note. Ratified by user in-session (ratify)."}
{"kind":"gate","cr":"github-8","gate":"impl","verdict":"approve","by":"unional","why":"cold sdd-impl-judge re-derived all 33 frozen scenarios independently and verified each; mutation-backstopped on reserved-key (forced always-false) + replace-by-name (forced always-append) — both caught, file restored; merged tree green (pnpm verify 295/295 across 18 files, rebased onto main c26b019); clean-architecture layering (pure domain / ConfigFs adapter / AXI cli) confirmed; no .feature/.agents/spec edits by the impl-producer. Post-gate: empty-state prints (none) + removed outdated cli-command.md governance, both re-verified green. Ratified by user in-session (ratify)."}
{"kind":"followup","cr":"github-8","class":"backlog","note":"Adopt config add in plugin install/init scripts once version-locking (npx universal-plugin@<version> pinning) is confirmed. The command ships now; safe install-script adoption is gated on the pinning mechanism per the issue's Blocker. New territory, contradicts no completion claim this CR made."}
